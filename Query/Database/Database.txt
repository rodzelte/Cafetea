-- 1. Create the Roles table
CREATE TABLE Roles (
    RoleID INT PRIMARY KEY IDENTITY(1,1),
    RoleName NVARCHAR(20) NOT NULL -- 'Superadmin', 'Staff', 'Client'
);

-- 2. Create the Users table
CREATE TABLE Users (
    UserID INT PRIMARY KEY IDENTITY(1,1),
    FullName NVARCHAR(100),
    Email NVARCHAR(100) UNIQUE NOT NULL,
    PasswordHash NVARCHAR(MAX) NOT NULL, -- Never store plain text!
    RoleID INT,
    CreatedAt DATETIME DEFAULT GETDATE(),
    CONSTRAINT FK_UserRole FOREIGN KEY (RoleID) REFERENCES Roles(RoleID)
);

-- 3. Seed the Roles
INSERT INTO Roles (RoleName) VALUES ('Superadmin'), ('Staff'), ('Client');

-- Add the new column
ALTER TABLE Roles ADD RoleCode NVARCHAR(10) NULL;

-- Update the existing rows with your specific codes
UPDATE Roles SET RoleCode = 'ADMIN' WHERE RoleName = 'Superadmin';
UPDATE Roles SET RoleCode = 'STAFF' WHERE RoleName = 'Staff';
UPDATE Roles SET RoleCode = 'CLNT'  WHERE RoleName = 'Client';

-- Optional: Make it NOT NULL now that it's populated
ALTER TABLE Roles ALTER COLUMN RoleCode NVARCHAR(10) NOT NULL;